@Library('my-shared-lib') _

pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Mamatha1206/shared-library-repo'
            }
        }
        stage('Build') {
            steps {
                mavenBuild('clean package')
            }
        }
    }
}
